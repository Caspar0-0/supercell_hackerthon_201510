# Voice Character Matcher Web - Requirements
# Flask web framework
Flask==3.0.0
flask-cors==4.0.0

# Audio processing
librosa==0.10.2
soundfile==0.12.1
audioread==3.0.1
numba==0.59.1

# Numerical computing (versions with binary wheels for Python 3.11)
numpy==1.26.4
scipy==1.11.4

# Additional dependencies
Werkzeug==3.0.1

# Audio format support (needed for WebM/OGG)
# Note: ffmpeg or avconv must be installed on the system
# Install via: brew install ffmpeg (Mac) or apt-get install ffmpeg (Linux)

# Production server
gunicorn==21.2.0
